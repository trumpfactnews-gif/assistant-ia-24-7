--- orig/app/src/main/java/com/example/ui/screens/QuantumScreen.kt	2026-07-05 23:01:30.000000000 +0000
+++ /tmp/claude-0/-home-user-assistant-ia-24-7/8e4202f4-6e36-5b4b-a417-8230f78f3e3e/scratchpad/secapp/app/src/main/java/com/example/ui/screens/QuantumScreen.kt	2026-07-05 23:37:45.559608940 +0000
@@ -335,6 +335,47 @@
                             }
                         }
                     }
+                    "LOCAL_REAL_QUANT" -> {
+                        // Emerald card: real on-device quantitative engine
+                        Card(
+                            colors = CardDefaults.cardColors(containerColor = Color(0xFF064E3B)),
+                            border = BorderStroke(1.dp, Color(0xFF10B981)),
+                            modifier = Modifier.fillMaxWidth()
+                        ) {
+                            Column(
+                                modifier = Modifier.padding(14.dp).fillMaxWidth(),
+                                horizontalAlignment = Alignment.CenterHorizontally,
+                                verticalArrangement = Arrangement.spacedBy(6.dp)
+                            ) {
+                                Row(
+                                    verticalAlignment = Alignment.CenterVertically,
+                                    horizontalArrangement = Arrangement.Center
+                                ) {
+                                    Box(
+                                        modifier = Modifier
+                                            .size(10.dp)
+                                            .clip(RoundedCornerShape(5.dp))
+                                            .background(Color(0xFF34D399))
+                                    )
+                                    Spacer(modifier = Modifier.width(8.dp))
+                                    Text(
+                                        text = "MOTEUR QUANTITATIF LOCAL – CALCUL RÉEL (OHLCV 5 ANS)",
+                                        fontSize = 11.sp,
+                                        fontWeight = FontWeight.Black,
+                                        color = Color(0xFFD1FAE5),
+                                        letterSpacing = 0.5.sp
+                                    )
+                                }
+                                Text(
+                                    text = "Prédictions calculées sur l'appareil à partir de 5 ans de données réelles : analogues historiques, régression logistique et Monte Carlo. Aucun biais codé en dur, aucune génération par IA.",
+                                    fontSize = 11.sp,
+                                    color = Color(0xFFA7F3D0),
+                                    textAlign = androidx.compose.ui.text.style.TextAlign.Center,
+                                    lineHeight = 16.sp
+                                )
+                            }
+                        }
+                    }
                 }
 
                 // Sourcing Checklist Card
@@ -370,6 +411,7 @@
                                     text = when (provenance) {
                                         "LOCAL_SIMULATED" -> "MODÈLE DE SECOURS (SIMULÉ HORS-LIGNE)"
                                         "GEMINI_GENERATED" -> "INFÉRENCE IA DISTANTE (GEMINI 3.5 FLASH)"
+                                        "LOCAL_REAL_QUANT" -> "CALCUL LOCAL RÉEL (OHLCV 5 ANS)"
                                         else -> "SERVEUR API DISTANT (RÉEL VPS)"
                                     },
                                     fontSize = 8.sp,
@@ -408,6 +450,7 @@
                         text = when (provenance) {
                             "LOCAL_SIMULATED" -> "Basé sur 10 000 états historiques simulés (VAE + FAISS local)"
                             "GEMINI_GENERATED" -> "Génération probabiliste par grand modèle de langage (${currentPrediction.nNeighbors} voisins textuels)"
+                            "LOCAL_REAL_QUANT" -> "Basé sur ${currentPrediction.nNeighbors} états historiques analogues réels (données 5 ans, calcul local)"
                             else -> "Basé sur ${currentPrediction.nNeighbors} états historiques identiques (2016–2026)"
                         },
                         style = MaterialTheme.typography.bodySmall,
@@ -645,26 +688,39 @@
                         verticalArrangement = Arrangement.spacedBy(8.dp)
                     ) {
                         Text(
-                            "Informations de Calcul & VAE",
+                            when (provenance) {
+                                "LOCAL_REAL_QUANT" -> "Informations de Calcul"
+                                else -> "Informations de Calcul & VAE"
+                            },
                             style = MaterialTheme.typography.labelSmall,
                             fontWeight = FontWeight.Bold,
                             color = MaterialTheme.colorScheme.secondary
                         )
-                        Text(
-                            "• Encodeur VAE : Réduction de 128 dimensions brutes de marché vers un espace latent compact en 64 dimensions.",
-                            style = MaterialTheme.typography.bodySmall,
-                            color = MaterialTheme.colorScheme.onSurfaceVariant
-                        )
-                        Text(
-                            "• Recherche HNSW : Recherche des voisins les plus proches effectuée dans l'index FAISS de 50 millions d'états.",
-                            style = MaterialTheme.typography.bodySmall,
-                            color = MaterialTheme.colorScheme.onSurfaceVariant
-                        )
-                        Text(
-                            "• Non-Subjectivité : Aucune opinion qualitative ou IA n'est appliquée ici. Ces pourcentages reflètent purement les occurrences d'évolution empirique observées dans le passé historique (2016–2026).",
-                            style = MaterialTheme.typography.bodySmall,
-                            color = MaterialTheme.colorScheme.onSurfaceVariant
-                        )
+                        // Méthodologie affichée selon la provenance réelle (honnêteté).
+                        val methodoLines = when (provenance) {
+                            "LOCAL_REAL_QUANT" -> listOf(
+                                "• Analogues de régime : rendements des jours passés partageant la même tendance, la même volatilité et la même zone RSI que le jour courant.",
+                                "• Régression logistique + Monte Carlo : direction estimée par un modèle entraîné sur les indicateurs (RSI, MACD, Bollinger, EMA, ATR), complétée par un bootstrap des rendements réels.",
+                                "• Non-Subjectivité : aucune opinion ni IA générative. Les pourcentages reflètent des occurrences empiriques réelles sur 5 ans d'OHLCV."
+                            )
+                            "GEMINI_GENERATED" -> listOf(
+                                "• Génération par IA : distribution estimée par le modèle Gemini à partir de l'historique et du contexte du marché.",
+                                "• Fallback : le moteur local et le serveur quantitatif principal étaient injoignables.",
+                                "• Prudence : sortie d'un grand modèle de langage, à recouper avec d'autres sources."
+                            )
+                            else -> listOf(
+                                "• Encodeur VAE : Réduction de 128 dimensions brutes de marché vers un espace latent compact en 64 dimensions.",
+                                "• Recherche HNSW : Recherche des voisins les plus proches effectuée dans l'index FAISS de 50 millions d'états.",
+                                "• Non-Subjectivité : Aucune opinion qualitative ou IA n'est appliquée ici. Ces pourcentages reflètent purement les occurrences d'évolution empirique observées dans le passé historique (2016–2026)."
+                            )
+                        }
+                        methodoLines.forEach { line ->
+                            Text(
+                                line,
+                                style = MaterialTheme.typography.bodySmall,
+                                color = MaterialTheme.colorScheme.onSurfaceVariant
+                            )
+                        }
                     }
                 }
 
@@ -720,7 +776,7 @@
         modifier = modifier
     ) {
         Text(
-            text = "source: VAE + FAISS | fraîcheur: $timeStr",
+            text = "mise à jour: $timeStr",
             style = MaterialTheme.typography.labelSmall,
             fontWeight = FontWeight.Bold,
             color = MaterialTheme.colorScheme.onPrimaryContainer,
